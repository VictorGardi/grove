---
id: T-158
title: update task switcher to look like home page
status: done
created: '2026-04-11T07:15:04.201Z'
execSessionAgent: opencode
execModel: opencode/big-pickle
useWorktree: false
terminalExecContextSent: false
completed: '2026-04-11T10:27:49.027Z'
---

## Description

Update the table view in the Task Switcher modal (`src/renderer/src/components/shared/TaskSwitcherModal.tsx`) to match the column ordering of the HomePage TaskRow component (`src/renderer/src/components/Home/HomePage.tsx`):

**New column order (left to right):**

1. Running dot / status indicator (dot showing running/waiting/done state)
2. Task ID (separate element, e.g., `#T-123`)
3. Task title (separate element)
4. Workspace name
5. Status badge (backlog/doing/review/done)

**Changes required:**

- Split the combined `taskTitle + taskId` container into separate elements
- Reorder to match: indicator → ID → title → workspace → status
- Reuse the existing `renderStatus` function for the indicator and status badge

## Definition of Done

- [x] Task ID displayed as separate element (not combined with title)
- [x] Running/status dot indicator displayed as first column
- [x] Column order matches HomePage: dot → ID → title → workspace → status
- [x] Column element structure matches HomePage TaskRow (5 separate span elements)
- [x] Long task titles use text-overflow: ellipsis matching HomePage behavior

## Context for agent
