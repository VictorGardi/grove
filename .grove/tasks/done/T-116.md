---
id: T-116
title: all tasks i move to done show up as lingering session in agent view
status: done
created: '2026-04-09T06:24:49.616Z'
execSessionAgent: opencode
execModel: opencode/big-pickle
useWorktree: false
terminalExecSession: grove-term-c0e897-T-116-exec
terminalExecContextSent: true
completed: '2026-04-09T11:05:08.058Z'
terminalPlanSession: grove-term-c0e897-T-116-plan
---

## Description

When tasks (T-113, T-114, T-115) are moved to "done" status, the tmux sessions still exist and show as "HUNG" in AgentMonitor - they're not being killed at all. The `handleDragToDone` function should kill both exec and plan sessions, but the kills are either failing or not being triggered.

The current logic in `classifyStatus` marks sessions as "HUNG" when `taskStatus === "done"`, which is confusing - if a session still exists for a done task, it should show as "COMPLETED" (green) to indicate the session properly finished before the task was marked done. If the session was supposed to be killed but wasn't, that's a separate bug.

For now, fix the classification to show "COMPLETED" for done tasks (sessions were intentionally kept until task completion), and ensure the kill logic in Board.tsx is working.

## Definition of Done

- [x] Kill both `terminalExecSession` and `terminalPlanSession` when task is dragged to done
- [x] Clear both session fields from task frontmatter after killing
- [x] Show sessions with taskStatus === "done" as "COMPLETED" (green) instead of "HUNG" (red)
- [ ] Test: verify sessions are killed when task moves to done (tmux list-sessions should not show them)
- [ ] Test: moving a task to review keeps sessions intact (should show as LINGERING)

## Context for agent
