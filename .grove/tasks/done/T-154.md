---
id: T-154
title: refactor and optimize
status: done
created: '2026-04-11T06:49:50.088Z'
execSessionAgent: opencode
execModel: opencode/big-pickle
useWorktree: false
terminalExecContextSent: false
completed: '2026-04-11T07:07:10.825Z'
---

## Description

Refactor and optimize the codebase to remove duplication and fix naming conflicts:

### 1. Rename duplicate BoardState interfaces

- Rename `BoardState` in `src/renderer/src/stores/useBoardStore.ts` → `SearchState` (represents search UI state: searchQuery, searchActive, focusedTaskId)
- Rename `BoardState` in `src/renderer/src/stores/useWorkspaceStore.ts` → `TaskDetailState` (represents task detail panel state: selectedTaskId, selectedTaskBody, taskDetailTab, scrollPosition)

### 2. Extract shared task enrichment logic

The same task enrichment logic (adding workspacePath, workspaceName, isRunning, execTmuxAlive, planTmuxAlive, execAgentState, planAgentState, lastViewedAt) exists in both:

- `useAllTasksStore.ts`: `TaskWithWorkspace` interface and `getAllTasksGrouped()` function
- `useTaskSwitcherStore.ts`: `SortedTask` interface and `getSortedTasks()` function

Extract the shared enrichment logic into a reusable helper function (e.g., `enrichTaskWithWorkspace()`) that both functions can use, rather than duplicating the field mapping logic.

## Definition of Done

- [x] Rename `BoardState` → `SearchState` in useBoardStore.ts and update all usages
- [x] Rename `BoardState` → `TaskDetailState` in useWorkspaceStore.ts and update all usages
- [x] Extract shared task enrichment logic into a reusable helper function
- [x] Update getAllTasksGrouped and getSortedTasks to use the shared helper
- [x] Verify TypeScript compiles without errors (npm run typecheck)
- [x] Verify the app builds successfully (npm run build)

## Context for agent
