---
id: T-097
title: clean up & refactor
status: done
created: '2026-04-07T18:11:51.850Z'
planTmuxSession: grove-plan-c0e897-T-097
planSessionId: ses_296d9499effeVS3fEACiXsRsxt
planSessionAgent: opencode
execSessionAgent: opencode
execModel: opencode/big-pickle
useWorktree: false
execTmuxSession: grove-exec-c0e897-T-097
execSessionId: ses_296cf5d3dffe4DBRg7OwKHRqaF
completed: '2026-04-07T18:52:20.794Z'
---

## Description

Refactor the Grove codebase to eliminate concrete code duplications and fix structural issues. All changes are incremental and safe — no architectural rewrites. Target areas: main-process utilities shared between `planManager.ts` and `tmuxSupervisor.ts`, language-detection maps duplicated across `filesystem.ts` and `git.ts`, dead code in the renderer, debug artifacts, and type-safety gaps.

## Definition of Done

- [x] Extract `buildEnvPath()` into `src/main/env.ts`; replace all three call sites: `SpawnPlanRunner.start()` (planManager.ts), `TmuxSupervisor.start()` (tmuxSupervisor.ts), and `PlanManager.listModels()` (planManager.ts).
- [x] Extract `parseOpencodeLine()` and `parseCopilotLine()` into `src/main/agentOutputParser.ts`. Unify the two near-identical `parseOpencodeLine` variants via a `__grove_error` discriminated union return type. Remove the inline `obj.type === "error"` pre-check in `SpawnPlanRunner`'s readline handler (planManager.ts) and rely on the shared parser's return value instead.
- [x] Extract the write/cleanup logic from `writeOpencodeConfig`/`cleanupGroveConfig` into exported functions in `src/main/opencodeConfig.ts`. Functions must accept the caller's `wroteConfigFiles: Map<string, string>` as a parameter (state ownership stays with `SpawnPlanRunner` and `TmuxSupervisor`). Replace both class-level implementations.
- [x] Move `LANG_MAP`, `FILENAME_MAP`, and `detectLanguage()` from `filesystem.ts` into `src/shared/language.ts`; remove `LANG_MAP_GIT`, `FILENAME_MAP_GIT`, and `detectLanguageGit()` from `git.ts`; import and use the shared versions in both files.
- [x] Delete the dead copy at `src/renderer/src/utils/taskBodyParser.ts` (no renderer code imports it; the only consumer is `contextGenerator.ts` which already uses `@shared/taskBodyParser`).
- [x] Replace the `require('./tasks')` dynamic require in `tmuxSupervisor.ts` with constructor injection: add an optional `updateTask` callback parameter to `TmuxSupervisor`'s constructor. Guard the call site with the existing `if (tailer.taskFilePath)` check so reconnect paths (where `taskFilePath` is undefined) remain no-ops.
- [x] Remove the debug `console.log` from `readTaskBody` in `tasks.ts`.
- [x] Remove the local `STATUS_DIRS_SET` re-declaration in `writeTaskRaw` (`tasks.ts`); replace with the module-level `STATUS_DIRS` constant.
- [x] Add a `cancelAll()` method to `TmuxPlanRunner` that stores a workspace path per active key (in `runCallbacks` or a parallel map) so it can pass the correct path to each `cancel()` call. Remove the `this.tmuxRunner["activeKeys"]` private-field bypass in `PlanManager.cancelAll()`.
- [x] Rename `capturePan` → `capturePane` in `TmuxSupervisor` and update the single caller in `PlanManager.captureTmuxPane()`.
- [x] Remove the `as RunOpts` casts in `PlanManager.run()` — the object literals satisfy `RunOpts` structurally; resolve any real type gap instead. Change `PlanRunner.start()` to return `Promise<void>` and update `SpawnPlanRunner.start()` signature to match.
- [x] Extract `MAX_FILE_SIZE` into `src/shared/fileUtils.ts` and use from both `filesystem.ts` and `git.ts`. Export the Buffer-based `isBinary()` from the same module; adapt `readFileAtBranch` in `git.ts` to convert its string output to a Buffer before calling it, or keep a string-based null-byte check there and only share the constant.
- [x] All existing tests pass (`npm test`) after each change.

## Context for agent
