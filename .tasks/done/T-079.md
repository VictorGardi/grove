---
id: T-079
title: move between workspaces without task open/close
status: done
created: '2026-04-07T11:43:01.282Z'
planTmuxSession: grove-plan-c0e897-T-079
planSessionId: ses_2983deea3ffeFFqXs7sHie523b
planSessionAgent: opencode
planModel: opencode/minimax-m2.5-free
execSessionAgent: opencode
execModel: opencode/big-pickle
useWorktree: false
execSessionId: ses_29812a597ffeSe68U6JfgkW1k0
execTmuxSession: grove-exec-c0e897-T-079
completed: '2026-04-07T13:52:34.834Z'
---

## Description

When switching between workspaces using Cmd+digit shortcuts, preserve and restore the board view UI state (open task detail panel with its active tab and scroll position) and terminal panel state (open/closed) per-workspace, without persisting across app restarts.

## Definition of Done

- [x] Add per-workspace UI state storage to `useWorkspaceStore.ts`:
  - [x] `workspaceBoardStates: Record<string, { selectedTaskId: string | null, selectedTaskBody: string | null, taskDetailTab: 'edit' | 'plan' | 'changes' | 'debug', scrollPosition: number }>`
  - [x] `workspaceTerminalStates: Record<string, { terminalPanelOpen: boolean }>`
- [x] Modify `setActiveWorkspace` in `useWorkspaceStore.ts` to:
  - [x] Save current workspace's board and terminal state before switching
  - [x] Restore target workspace's board and terminal state after switching
- [x] Handle edge case: when restoring, verify the task exists in the new workspace's task list; if not, clear the selection gracefully
- [x] Move terminal panel state from `useNavStore` to workspace store (per-workspace terminal state)
- [x] Mark scroll position preservation as "best effort" - it may not sync perfectly with editor/preview sync logic
- [x] Verify: open task in workspace 1, switch to workspace 2 via Cmd+2, switch back to workspace 1 via Cmd+1 — task should still be open
- [x] Verify: terminal open in workspace 1, switch to workspace 2, switch back — terminal should be open
- [x] Verify: active tab (edit/plan/changes/debug) is preserved when switching back to workspace

## Context for agent

Current code references:

- `useWorkspaceStore.ts:81-88` - `setActiveWorkspace(path)` handles workspace switching
- `useDataStore.ts:101-150` - `selectedTaskId`, `selectedTaskBody`, `setSelectedTask()`
- `useNavStore.ts:8` - `terminalPanelOpen` is global state
- `useKeyboardShortcuts.ts:135-146` - Cmd+digit shortcuts already exist
- `App.tsx:50-58` - calls `clearData()` when workspace switches (need to preserve UI state before this clears)
